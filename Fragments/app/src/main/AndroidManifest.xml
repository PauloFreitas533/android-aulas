<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.wolneyhqf.android.aulas.fragments">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddFragmentActivity"
            android:label="AddFragmentActivity" />
        <activity android:name=".ReplaceFragmentActivity" />
        <activity android:name=".PaisesActivity" />
        <activity android:name=".PaisActivity" />
        <activity android:name=".PaisesFragmentActivity" />
        <activity android:name=".PaisFragmentActivity" />
        <activity android:name=".PaisesDualActivity"></activity>
    </application>

</manifest>